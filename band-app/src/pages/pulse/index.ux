<template>
  <stack class="root" onswipe="onSwipe">
    <div class="screen {{anim}}" if="{{cur === 'hr'}}">
      <div class="ambient" style="background: linear-gradient(180deg, rgba(90,200,250,0.2) 0%, rgba(0,0,0,0) 100%)"></div>
      <div class="ambient-fade"></div>
      <text class="caps pulse-caps">ПУЛС</text>
      <stack class="hr-wrap">
        <div class="glow {{hr > 0 ? 'breathe' : ''}}" style="width: 120px; height: 120px; background-color: {{zoneColor}}"></div>
        <image class="heart {{hr > 0 ? 'beat' : ''}}" src="/common/icons/heart.png" style="animation-duration: {{beatMs}}ms"></image>
      </stack>
      <text class="hr" style="color: {{zoneColor}}">{{hrText}}</text>
      <div class="zones">
        <div for="{{zoneBars}}" class="zseg {{$item.on ? 'zseg-on' : ''}}" style="background-color: {{$item.c}}"></div>
      </div>
      <text class="sub">{{limit}}{{pctText}}</text>
    </div>

    <div class="screen {{anim}}" if="{{cur === 'auto'}}">
      <text class="caps">АВТОКОНТРОЛ</text>
      <div class="btn btn-toggle {{ai ? 'off' : ''}} {{pressed ? 'down' : ''}}"
           style="margin-top: 36px; background: linear-gradient(180deg, {{en ? '#0A84FF' : '#2C2C2E'}} 0%, {{en ? '#0566CC' : '#1C1C1E'}} 100%)"
           ontouchstart="press" ontouchend="release" ontouchcancel="release" onclick="toggle">
        <div class="btn-shine"></div>
        <text class="toggle-text">{{en ? 'ВКЛ' : 'ИЗКЛ'}}</text>
      </div>
      <text class="sub" style="color: {{stateColor}}">{{stateText}}</text>
      <text class="factor" if="{{en && !ai}}" style="color: {{factorColor}}">сила {{sf}}%</text>
    </div>

    <div class="screen {{anim}}" if="{{cur === 'chart'}}">
      <text class="caps">ПОСЛЕДНИ 3 МИН</text>
      <div class="chart">
        <div for="{{bars}}" class="bar" style="height: {{$item.h}}px; background-color: {{$item.c}}"></div>
      </div>
      <div class="pair">
        <div class="stat">
          <text class="stat-num">{{avgText}}</text>
          <text class="stat-label">средно</text>
        </div>
        <div class="stat">
          <text class="stat-num stat-max">{{maxText}}</text>
          <text class="stat-label">връх</text>
        </div>
      </div>
    </div>

    <div class="dots">
      <div for="{{dots}}" class="pdot {{$item.on ? 'pdot-on' : ''}}" style="background-color: {{$item.on ? dotAccent : '#3A3A3C'}}"></div>
    </div>
  </stack>
</template>

<script>
import { ZONE, GREEN, AMBER, BLUE, zoneOf, mods, bind, setPages, swipe } from '../../common/ui.js'

export default {
  private: {
    ids: [],
    idsKey: '',
    idx: 0,
    cur: 'hr',
    dots: [],
    dotAccent: BLUE,
    anim: '',
    hr: 0,
    hrText: '--',
    zoneColor: '#FFFFFF',
    beatMs: 450,
    zoneBars: [],
    limit: 'лимит --',
    pctText: '',
    en: false,
    ai: false,
    stateText: '',
    stateColor: '#8E8E93',
    sf: 100,
    factorColor: GREEN,
    bars: [],
    avgText: '--',
    maxText: '--',
    pressed: false
  },

  onInit() {
    setPages(this, ['hr', 'auto', 'chart'], BLUE)
    this.unbind = bind(this)
  },

  onDestroy() {
    this.unbind()
  },

  onSwipe(e) {
    swipe(this, e)
  },

  update() {
    const s = this.$app.$def.state
    if (!s) {
      return
    }
    const hg = mods(s).hg
    const lim = hg.up || s.lim || 170
    const z = s.hr > 0 ? s.z || zoneOf(s.hr, lim) : 0
    this.hr = s.hr || 0
    this.hrText = s.hr > 0 ? String(s.hr) : '--'
    this.zoneColor = s.hr > 0 ? ZONE[z] : '#FFFFFF'
    this.beatMs = s.hr > 0 ? Math.round(30000 / s.hr) : 450
    const zb = []
    for (let i = 1; i <= 5; i++) {
      zb.push({ c: i <= z ? ZONE[i] : '#2C2C2E', on: i === z })
    }
    this.zoneBars = zb
    this.limit = 'лимит ' + lim
    this.pctText = s.hr > 0 ? ' · ' + Math.round((s.hr / lim) * 100) + '%' : ''

    this.en = !!hg.en
    this.ai = !!hg.ai
    if (hg.ai) {
      this.stateText = 'AI сесията управлява'
      this.stateColor = BLUE
    } else if (!hg.en) {
      this.stateText = 'пулсът не пипа силата'
      this.stateColor = '#8E8E93'
    } else if (hg.hold) {
      this.stateText = 'задържа силата'
      this.stateColor = AMBER
    } else {
      this.stateText = 'следи пулса'
      this.stateColor = GREEN
    }
    this.sf = hg.sf || 100
    this.factorColor = this.sf >= 100 ? GREEN : this.sf >= 85 ? AMBER : '#FF453A'

    const hh = s.hh || []
    let lo = 999
    let hi = 0
    hh.forEach((v) => {
      if (v > 0) {
        lo = Math.min(lo, v)
        hi = Math.max(hi, v)
      }
    })
    lo = Math.max(40, lo - 8)
    hi = Math.max(hi, lo + 20)
    this.bars = hh.map((v) => ({
      h: v > 0 ? Math.round(8 + ((v - lo) / (hi - lo)) * 150) : 4,
      c: v > 0 ? ZONE[zoneOf(v, lim)] : '#2C2C2E'
    }))
    this.avgText = s.avg > 0 ? String(s.avg) : '--'
    this.maxText = s.max > 0 ? String(s.max) : '--'
  },

  press() { this.pressed = true },
  release() { this.pressed = false },
  toggle() {
    if (this.ai) {
      return
    }
    this.en = !this.en
    this.$app.$def.command('hg_toggle')
  }
}
</script>

<style>
/* ---- common (scripts/gen-pages.py stamps this into every page) ---- */
/* XEMS Band v4 — shared design system (212 × 520 AMOLED) */
.root {
  width: 212px;
  height: 520px;
  background-color: #000000;
}
.screen {
  width: 212px;
  height: 520px;
  flex-direction: column;
  align-items: center;
  padding-top: 32px;
  background-color: #000000;
}
.ambient {
  width: 212px;
  height: 260px;
  position: absolute;
  top: 0px;
  left: 0px;
  opacity: 0.55;
}
.ambient-fade {
  width: 212px;
  height: 520px;
  position: absolute;
  top: 0px;
  left: 0px;
  background: linear-gradient(180deg, rgba(0, 0, 0, 0) 0%, #000000 72%);
}
.in-up {
  animation-name: inUp;
  animation-duration: 280ms;
  animation-timing-function: ease-out;
}
.in-down {
  animation-name: inDown;
  animation-duration: 280ms;
  animation-timing-function: ease-out;
}
.in-fade {
  animation-name: fadeIn;
  animation-duration: 220ms;
  animation-timing-function: ease-out;
}
.dots {
  width: 16px;
  height: 520px;
  margin-left: 196px;
  flex-direction: column;
  justify-content: center;
  align-items: center;
}
.pdot {
  width: 6px;
  height: 6px;
  margin-top: 5px;
  margin-bottom: 5px;
  border-radius: 3px;
  background-color: #3a3a3c;
}
.pdot-on {
  width: 8px;
  height: 24px;
  border-radius: 4px;
  background-color: #ffffff;
}
.caps {
  font-size: 20px;
  font-weight: bold;
  color: #636366;
  letter-spacing: 1px;
}
.sub {
  width: 196px;
  text-align: center;
  font-size: 22px;
  color: #aeaeb2;
  lines: 2;
  text-overflow: ellipsis;
}
.chip {
  height: 28px;
  padding-left: 12px;
  padding-right: 12px;
  border-radius: 14px;
  font-size: 18px;
  font-weight: bold;
  color: #000000;
}
.chip-outline {
  height: 28px;
  padding-left: 10px;
  padding-right: 10px;
  border-radius: 14px;
  border-width: 2px;
  font-size: 18px;
  font-weight: bold;
}
.down {
  transform: scale(0.92);
}
.btn {
  justify-content: center;
  align-items: center;
  transition-property: transform;
  transition-duration: 120ms;
}
.off {
  opacity: 0.28;
}
.surface {
  background-color: #1c1c1e;
  border-radius: 24px;
  border-width: 1px;
  border-color: #2c2c2e;
}
.surface-elevated {
  background-color: #2c2c2e;
  border-radius: 24px;
  border-width: 1px;
  border-color: #3a3a3c;
}
.glow {
  position: absolute;
  border-radius: 999px;
  opacity: 0.38;
}
.breathe {
  animation-name: breathe;
  animation-duration: 1500ms;
  animation-iteration-count: infinite;
  animation-direction: alternate;
  animation-timing-function: ease-in-out;
}
.btn-ring {
  position: absolute;
  border-radius: 999px;
  border-width: 2px;
  border-color: rgba(255, 255, 255, 0.12);
}
.btn-shine {
  position: absolute;
  width: 60%;
  height: 28%;
  top: 8%;
  border-radius: 999px;
  background-color: rgba(255, 255, 255, 0.14);
}
.btn-primary {
  border-width: 2px;
  border-color: rgba(255, 255, 255, 0.1);
}
.btn-surface {
  background-color: #2c2c2e;
  border-width: 2px;
  border-color: #3a3a3c;
}
.btn-pill {
  width: 180px;
  height: 108px;
  border-radius: 54px;
  background-color: #2c2c2e;
  border-width: 2px;
  border-color: #3a3a3c;
}
.btn-pill-accent {
  border-color: rgba(255, 255, 255, 0.08);
}
.pill-img {
  width: 52px;
  height: 52px;
}
.btn-toggle {
  width: 168px;
  height: 168px;
  border-radius: 84px;
  border-width: 3px;
  border-color: rgba(255, 255, 255, 0.12);
}
.toggle-text {
  font-size: 38px;
  font-weight: bold;
  color: #ffffff;
  letter-spacing: 2px;
}
.play-wrap {
  justify-content: center;
  align-items: center;
}
.play {
  border-width: 2px;
  border-color: rgba(255, 255, 255, 0.1);
}
.play-img {
  width: 54px;
  height: 54px;
}
.ring-box {
  justify-content: center;
  align-items: center;
}
.ring-track {
  background-color: #141416;
  border-width: 1px;
  border-color: #1c1c1e;
}
.ring-glow {
  position: absolute;
  border-radius: 999px;
  opacity: 0.22;
}
.tile {
  width: 94px;
  height: 88px;
  margin-left: 4px;
  margin-right: 4px;
  border-radius: 22px;
  background-color: #1c1c1e;
  border-width: 1px;
  border-color: #2c2c2e;
  flex-direction: column;
  justify-content: center;
  align-items: center;
}
.tile-num {
  font-size: 34px;
  font-weight: bold;
  color: #ffffff;
}
.tile-label {
  font-size: 20px;
  color: #8e8e93;
}
.tiles {
  flex-direction: row;
  margin-top: 10px;
}
.stat {
  width: 94px;
  height: 92px;
  margin-left: 4px;
  margin-right: 4px;
  border-radius: 22px;
  background-color: #1c1c1e;
  border-width: 1px;
  border-color: #2c2c2e;
  flex-direction: column;
  justify-content: center;
  align-items: center;
}
.stat-num {
  font-size: 36px;
  font-weight: bold;
  color: #ffffff;
}
.stat-label {
  font-size: 20px;
  color: #8e8e93;
}
.stat-max {
  color: #ff453a;
}
.kcal {
  color: #ff9f0a;
}
.zstack {
  width: 192px;
  height: 16px;
  margin-top: 18px;
  border-radius: 8px;
  flex-direction: row;
  background-color: #141416;
  border-width: 1px;
  border-color: #1c1c1e;
}
.zpart {
  height: 16px;
}
.stop-box {
  justify-content: center;
  align-items: center;
}
.stop-ring {
  stroke-width: 12px;
  background-color: #1a0808;
  start-angle: 0deg;
  total-angle: 360deg;
}
.stop-core {
  border-width: 3px;
  border-color: rgba(255, 255, 255, 0.15);
  justify-content: center;
  align-items: center;
}
.stop-img {
  width: 58px;
  height: 58px;
}
.pair {
  flex-direction: row;
}
@keyframes inUp {
  from {
    transform: translateY(72px);
    opacity: 0;
  }
  to {
    transform: translateY(0px);
    opacity: 1;
  }
}
@keyframes inDown {
  from {
    transform: translateY(-72px);
    opacity: 0;
  }
  to {
    transform: translateY(0px);
    opacity: 1;
  }
}
@keyframes fadeIn {
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}
@keyframes breathe {
  from {
    transform: scale(0.88);
    opacity: 0.55;
  }
  to {
    transform: scale(1.14);
    opacity: 0.12;
  }
}
@keyframes beat {
  from {
    transform: scale(1);
  }
  to {
    transform: scale(1.22);
  }
}
@keyframes wave {
  from {
    transform: scale(0.55);
    opacity: 0.85;
  }
  to {
    transform: scale(1.08);
    opacity: 0;
  }
}
@keyframes spin {
  from {
    transform: rotate(0deg);
  }
  to {
    transform: rotate(360deg);
  }
}
/* ---- page ---- */
.pulse-caps {
  color: #5ac8fa;
}
.hr-wrap {
  width: 120px;
  height: 56px;
  margin-top: 24px;
  justify-content: center;
  align-items: center;
}
.heart {
  width: 46px;
  height: 46px;
}
.beat {
  animation-name: beat;
  animation-iteration-count: infinite;
  animation-direction: alternate;
  animation-timing-function: ease-in-out;
}
.hr {
  font-size: 104px;
  font-weight: bold;
  margin-top: 4px;
}
.zones {
  flex-direction: row;
  align-items: center;
  height: 20px;
  margin-top: 8px;
}
.zseg {
  width: 32px;
  height: 8px;
  margin-left: 2px;
  margin-right: 2px;
  border-radius: 4px;
}
.zseg-on {
  height: 18px;
  border-radius: 9px;
  border-width: 1px;
  border-color: rgba(255, 255, 255, 0.2);
}
.sub {
  margin-top: 14px;
}
.factor {
  margin-top: 14px;
  font-size: 30px;
  font-weight: bold;
}
.chart {
  width: 190px;
  height: 168px;
  margin-top: 24px;
  flex-direction: row;
  align-items: flex-end;
  padding-bottom: 4px;
  border-bottom-width: 1px;
  border-bottom-color: #2c2c2e;
}
.bar {
  width: 4px;
  margin-left: 1px;
  margin-right: 1px;
  border-radius: 2px;
}
.pair {
  margin-top: 24px;
}
</style>
