<template>
  <stack class="root" onswipe="onSwipe">
    <div class="screen in-fade">
      <div class="ambient" style="background: linear-gradient(180deg, rgba(48,209,88,0.18) 0%, rgba(0,0,0,0) 100%)"></div>
      <div class="ambient-fade"></div>
      <div class="check">
        <div class="btn-shine"></div>
        <image class="check-img" src="/common/icons/check.png"></image>
      </div>
      <text class="caps kind">{{kindText}}</text>
      <text class="dur">{{durText}}</text>
      <text class="kcal">{{kcal}} kcal</text>
      <div class="pair">
        <div class="stat">
          <text class="stat-num">{{avg}}</text>
          <text class="stat-label">средно</text>
        </div>
        <div class="stat">
          <text class="stat-num stat-max">{{max}}</text>
          <text class="stat-label">връх</text>
        </div>
      </div>
      <div class="zstack">
        <div for="{{zones}}" class="zpart" style="flex: {{$item.w}}; background-color: {{$item.c}}"></div>
      </div>
      <div class="btn done {{pressed ? 'down' : ''}}" ontouchstart="press" ontouchend="release" ontouchcancel="release" onclick="close">
        <div class="btn-shine"></div>
        <text class="done-text">Готово</text>
      </div>
    </div>
  </stack>
</template>

<script>
import router from '@system.router'
import { ZONE, hms, swipe } from '../../common/ui.js'

export default {
  private: {
    ids: ['main'],
    idsKey: 'main',
    idx: 0,
    cur: 'main',
    dots: [],
    anim: '',
    kindText: 'КРАЙ',
    durText: '0:00',
    kcal: '--',
    avg: '--',
    max: '--',
    zones: [],
    pressed: false
  },

  onInit() {
    const s = this.$app.$def.summary || {}
    this.kindText = s.kind === 'ai' ? 'AI СЕСИЯТА ПРИКЛЮЧИ' : 'ТРЕНИРОВКАТА ПРИКЛЮЧИ'
    this.durText = hms(s.dur || 0)
    this.kcal = s.kcal > 0 ? String(s.kcal) : '--'
    this.avg = s.avg > 0 ? String(s.avg) : '--'
    this.max = s.max > 0 ? String(s.max) : '--'
    const zt = s.zt || [0, 0, 0, 0, 0]
    const total = zt.reduce((a, b) => a + b, 0)
    this.zones = zt.map((v, i) => ({ w: total > 0 ? Math.max(v, 0.0001) : 1, c: ZONE[i + 1] }))
  },

  onSwipe(e) {
    swipe(this, e)
  },

  press() { this.pressed = true },
  release() { this.pressed = false },
  close() { router.back() }
}
</script>

<style>
/* ---- common (scripts/gen-pages.py stamps this into every page) ---- */
/* XEMS Band v4 — shared design system (212 × 520 AMOLED) */
.root {
  width: 212px;
  height: 520px;
  background-color: #000000;
}
.screen {
  width: 212px;
  height: 520px;
  flex-direction: column;
  align-items: center;
  padding-top: 32px;
  background-color: #000000;
}
.ambient {
  width: 212px;
  height: 260px;
  position: absolute;
  top: 0px;
  left: 0px;
  opacity: 0.55;
}
.ambient-fade {
  width: 212px;
  height: 520px;
  position: absolute;
  top: 0px;
  left: 0px;
  background: linear-gradient(180deg, rgba(0, 0, 0, 0) 0%, #000000 72%);
}
.in-up {
  animation-name: inUp;
  animation-duration: 280ms;
  animation-timing-function: ease-out;
}
.in-down {
  animation-name: inDown;
  animation-duration: 280ms;
  animation-timing-function: ease-out;
}
.in-fade {
  animation-name: fadeIn;
  animation-duration: 220ms;
  animation-timing-function: ease-out;
}
.dots {
  width: 16px;
  height: 520px;
  margin-left: 196px;
  flex-direction: column;
  justify-content: center;
  align-items: center;
}
.pdot {
  width: 6px;
  height: 6px;
  margin-top: 5px;
  margin-bottom: 5px;
  border-radius: 3px;
  background-color: #3a3a3c;
}
.pdot-on {
  width: 8px;
  height: 24px;
  border-radius: 4px;
  background-color: #ffffff;
}
.caps {
  font-size: 20px;
  font-weight: bold;
  color: #636366;
  letter-spacing: 1px;
}
.sub {
  width: 196px;
  text-align: center;
  font-size: 22px;
  color: #aeaeb2;
  lines: 2;
  text-overflow: ellipsis;
}
.chip {
  height: 28px;
  padding-left: 12px;
  padding-right: 12px;
  border-radius: 14px;
  font-size: 18px;
  font-weight: bold;
  color: #000000;
}
.chip-outline {
  height: 28px;
  padding-left: 10px;
  padding-right: 10px;
  border-radius: 14px;
  border-width: 2px;
  font-size: 18px;
  font-weight: bold;
}
.down {
  transform: scale(0.92);
}
.btn {
  justify-content: center;
  align-items: center;
  transition-property: transform;
  transition-duration: 120ms;
}
.off {
  opacity: 0.28;
}
.surface {
  background-color: #1c1c1e;
  border-radius: 24px;
  border-width: 1px;
  border-color: #2c2c2e;
}
.surface-elevated {
  background-color: #2c2c2e;
  border-radius: 24px;
  border-width: 1px;
  border-color: #3a3a3c;
}
.glow {
  position: absolute;
  border-radius: 999px;
  opacity: 0.38;
}
.breathe {
  animation-name: breathe;
  animation-duration: 1500ms;
  animation-iteration-count: infinite;
  animation-direction: alternate;
  animation-timing-function: ease-in-out;
}
.btn-ring {
  position: absolute;
  border-radius: 999px;
  border-width: 2px;
  border-color: rgba(255, 255, 255, 0.12);
}
.btn-shine {
  position: absolute;
  width: 60%;
  height: 28%;
  top: 8%;
  border-radius: 999px;
  background-color: rgba(255, 255, 255, 0.14);
}
.btn-primary {
  border-width: 2px;
  border-color: rgba(255, 255, 255, 0.1);
}
.btn-surface {
  background-color: #2c2c2e;
  border-width: 2px;
  border-color: #3a3a3c;
}
.btn-pill {
  width: 180px;
  height: 108px;
  border-radius: 54px;
  background-color: #2c2c2e;
  border-width: 2px;
  border-color: #3a3a3c;
}
.btn-pill-accent {
  border-color: rgba(255, 255, 255, 0.08);
}
.pill-img {
  width: 52px;
  height: 52px;
}
.btn-toggle {
  width: 168px;
  height: 168px;
  border-radius: 84px;
  border-width: 3px;
  border-color: rgba(255, 255, 255, 0.12);
}
.toggle-text {
  font-size: 38px;
  font-weight: bold;
  color: #ffffff;
  letter-spacing: 2px;
}
.play-wrap {
  justify-content: center;
  align-items: center;
}
.play {
  border-width: 2px;
  border-color: rgba(255, 255, 255, 0.1);
}
.play-img {
  width: 54px;
  height: 54px;
}
.ring-box {
  justify-content: center;
  align-items: center;
}
.ring-track {
  background-color: #141416;
  border-width: 1px;
  border-color: #1c1c1e;
}
.ring-glow {
  position: absolute;
  border-radius: 999px;
  opacity: 0.22;
}
.tile {
  width: 94px;
  height: 88px;
  margin-left: 4px;
  margin-right: 4px;
  border-radius: 22px;
  background-color: #1c1c1e;
  border-width: 1px;
  border-color: #2c2c2e;
  flex-direction: column;
  justify-content: center;
  align-items: center;
}
.tile-num {
  font-size: 34px;
  font-weight: bold;
  color: #ffffff;
}
.tile-label {
  font-size: 20px;
  color: #8e8e93;
}
.tiles {
  flex-direction: row;
  margin-top: 10px;
}
.stat {
  width: 94px;
  height: 92px;
  margin-left: 4px;
  margin-right: 4px;
  border-radius: 22px;
  background-color: #1c1c1e;
  border-width: 1px;
  border-color: #2c2c2e;
  flex-direction: column;
  justify-content: center;
  align-items: center;
}
.stat-num {
  font-size: 36px;
  font-weight: bold;
  color: #ffffff;
}
.stat-label {
  font-size: 20px;
  color: #8e8e93;
}
.stat-max {
  color: #ff453a;
}
.kcal {
  color: #ff9f0a;
}
.zstack {
  width: 192px;
  height: 16px;
  margin-top: 18px;
  border-radius: 8px;
  flex-direction: row;
  background-color: #141416;
  border-width: 1px;
  border-color: #1c1c1e;
}
.zpart {
  height: 16px;
}
.stop-box {
  justify-content: center;
  align-items: center;
}
.stop-ring {
  stroke-width: 12px;
  background-color: #1a0808;
  start-angle: 0deg;
  total-angle: 360deg;
}
.stop-core {
  border-width: 3px;
  border-color: rgba(255, 255, 255, 0.15);
  justify-content: center;
  align-items: center;
}
.stop-img {
  width: 58px;
  height: 58px;
}
.pair {
  flex-direction: row;
}
@keyframes inUp {
  from {
    transform: translateY(72px);
    opacity: 0;
  }
  to {
    transform: translateY(0px);
    opacity: 1;
  }
}
@keyframes inDown {
  from {
    transform: translateY(-72px);
    opacity: 0;
  }
  to {
    transform: translateY(0px);
    opacity: 1;
  }
}
@keyframes fadeIn {
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}
@keyframes breathe {
  from {
    transform: scale(0.88);
    opacity: 0.55;
  }
  to {
    transform: scale(1.14);
    opacity: 0.12;
  }
}
@keyframes beat {
  from {
    transform: scale(1);
  }
  to {
    transform: scale(1.22);
  }
}
@keyframes wave {
  from {
    transform: scale(0.55);
    opacity: 0.85;
  }
  to {
    transform: scale(1.08);
    opacity: 0;
  }
}
@keyframes spin {
  from {
    transform: rotate(0deg);
  }
  to {
    transform: rotate(360deg);
  }
}
/* ---- page ---- */
.screen {
  padding-top: 28px;
}
.check {
  width: 76px;
  height: 76px;
  border-radius: 38px;
  background: linear-gradient(180deg, #30d158 0%, #1f9a42 100%);
  justify-content: center;
  align-items: center;
  border-width: 2px;
  border-color: rgba(255, 255, 255, 0.15);
}
.check-img {
  width: 42px;
  height: 42px;
}
.kind {
  margin-top: 12px;
  width: 200px;
  text-align: center;
  font-size: 18px;
  color: #aeaeb2;
}
.dur {
  font-size: 64px;
  font-weight: bold;
  color: #ffffff;
  margin-top: 4px;
}
.kcal {
  font-size: 28px;
  font-weight: bold;
  color: #ff9f0a;
  margin-top: 2px;
}
.pair {
  margin-top: 12px;
}
.zstack {
  margin-top: 14px;
}
.done {
  width: 190px;
  height: 84px;
  margin-top: 18px;
  border-radius: 42px;
  background: linear-gradient(180deg, #3a3a3c 0%, #2c2c2e 100%);
  border-width: 2px;
  border-color: #48484a;
}
.done-text {
  font-size: 28px;
  font-weight: bold;
  color: #ffffff;
}
</style>
