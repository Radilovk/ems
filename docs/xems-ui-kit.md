# XEMS UI kit — таймер, плейър, пулс, AI, навигация (v1.1.66)

Код: `branding/java/src/com/isaigu/gymapp/widget/XemsUi.java`. Компилира се с `compile-music-sync-java.sh`, инсталира се от `apply-music-player.py`.

## Принцип

Всички модули ползват едни и същи цветове и компоненти, взети от темата на приложението (`values` / `values-night`). Така светлата и тъмната тема работят автоматично.

| Токен | Цвят в темата | Употреба |
|---|---|---|
| Повърхности | `bg_card`, `bg_surface`, `card_stroke` | Карти, полета, фина рамка — както картите на екрана за тренировка |
| Акцент | `accent_primary` (червено) | Избран таб, марка, номер на блок — както таба „Тренировка“ |
| Старт | `ma_badge_active_bg` / `impulse_accent` (зелено) | Активирай, Play, избрани стойности — както бутона за старт и плъзгачите |
| Пауза | `ma_badge_pause_bg` (кехлибарено) | Състояния „готов“ и „пауза“ |
| Текст | `text_primary`, `text_secondary`, `text_hint` | Навсякъде |

Компоненти: карта, повърхност, бутон (зелен / червен / вторичен / текстов), кръгъл бутон с икона, чип, сегментиран избор (като таб лентата), брояч −/+ (задържане = бързо), ред с превключвател, значка за състояние, прозорец с заглавие, значка, i и ✕, превъртащо се съдържание и фиксирани бутони отдолу.

Взаимодействие: вълна при докосване и леко свиване при натиск, кратка вибрация, броячите ускоряват при задържане.

## Интервален таймер

Настройките вече са изградени в Java — без XML, без падащи менюта, без скрития панел „Звук и запазени“.

- **Интервал | Програми** — сегментиран избор.
- **Интервал**:
  - два брояча: интервал (стъпка 5 s до 1 мин, 15 s до 3 мин, после 30 s) и повторения (∞ … 30);
  - бързи чипове: 0:20 … 5:00 и ∞, 4, 6, 8, 10, 12;
  - лента с по един сегмент на повторение (зелено → червено) и сумарно време.
- **Програми (блокове)**:
  - всеки блок е карта с цикли, време, сила %, Hz, µs и мини-индикатор за силата;
  - „Повтаряй до края на времето“ (RPT) е превключвател;
  - времето за тренировка е до 90 мин (преди беше ограничено до 10 мин от полето на интервала).
- **Запазени програми** — чипове:
  - докосване зарежда;
  - задържане отваря „Преименувай / Изтрий“;
  - „+ Запази“ пита за име, а „✓ Обнови“ презаписва избраната.
- **Сигнал** — чипове: докосването избира и веднага пуска звука. „Телефон“ и „Файл“ отварят избор; избраният файл има ▶ и ✕.
- **Долу**: „▶ Активирай“. Докато таймерът работи: „Изключи таймера“ и „Приложи“.
- **Значка** в заглавието: Изключен / Готов — чака старт / ● Работи / Пауза.

**Редактор на блокове**:
- карти с броячи за цикли, сила, Hz и µs;
- дублиране (⧉) и премахване (✕) на блок;
- общо време в значката;
- „Готово“ записва, „Откажи“ отхвърля.

Циферблатът е описан в `TimerRingView`: сегментиран пръстен, опашка, сияние.

## Плейър — компактен режим (v1.1.63)

По подразбиране плейърът се отваря като **същия плаващ циферблат като таймера и пулса** (`MusicDial`):
- пръстенът показва докъде е стигнала песента; влачене по пръстена превърта;
- в пръстена е визуализаторът;
- в центъра е голямо време, а под него името на песента;
- **докосване в средата = пусни / пауза**; влачене в средата мести циферблата;
- бутоните около пръстена са на същите позиции и в същите цветове: ✕ (червен, 1:30), ⏭ (зелен, 3:00), ☰ (жълт, 4:30, отваря пълния плейър).

Пълният плейър има „—“ в заглавието, което го свива обратно в циферблат. Музиката продължава и при двете смени.

## Плейър — пълен режим

Разположението (XML) и кръговото търсене се запазват. Отгоре се прилага комплектът:
- голям зелен кръгъл ▶;
- кръгли бутони ☰ / ⚙ / i / ✕ (отворен панел = зелен);
- профилите Мек / Балансиран / Ритъм са чипове;
- четирите настройки са броячи на комплекта (вместо старите `AmountView`).

Ново:
- **⏮ / ⏭** до времето — ако песента свири, продължава със следващата;
- **✕ на всеки ред** в плейлистата — изтриване;
- текущата песен е маркирана в зелено с ▶.

## Пулс

Прозорецът на ♥ ползва бутоните и значката на комплекта. Циферблатът е `TimerRingView` (зона, коридор, таван, пулсиране с пулса).

## Принцип „само необходимото“ (v1.1.66)

На екрана е само това, което се пипа най-често. Детайлите се отварят при нужда, във втори прозорец.

- **Таймер:**
  - Интервал и Повторения са два реда: име вляво, брояч с фиксирана ширина вдясно. Така нищо не се застъпва.
  - Под тях има един ред „Общо 4:00“.
  - Махнати са бързите чипове, лентата с повторенията и подзаглавието.
  - „Сигнал ›“ и „Запазени програми ›“ са редове, които отварят отделен прозорец.
  - При програмите с блокове е махнат индикаторът за сила и подсказките.
- **Пулс:** редът с MAC/ключ се показва само докато гривната не е настроена.

## Език

`widget/XemsLang` чете езика на приложението (`setting_share` → `language`), не езика на системата. Преди, при таблет на английски, добавките излизаха на английски и в българската версия. Всички `tr()` / `t()` минават през него.

## Главен панел (`widget/XemsPanel`)

Отгоре надолу:
- ■ Стоп (квадрат, червен);
- ▶ Старт / ❚❚ Пауза (висок, зелен / кехлибарен);
- + (висок);
- − (висок);
- ⚙ Главен (квадрат).

Трите високи бутона делят поравно височината между двата квадрата. Всеки нов бутон натиска скрития оригинален, така че логиката на тренировката е същата. Иконите са векторни (`widget/XemsIcon`).

## Меню ☰

Иконите на страниците са от същото семейство (`XemsIcon`), в цветен кръг като плочките на модулите. Текущата страница е с плътен кръг.

## Цял екран (`widget/XemsFullscreen`)

Горната и долната системна лента са скрити. Плъзгане от ръба ги показва за момент, после се скриват сами. Прозорците на добавките пазят режима.

## Пулс модул по подразбиране

- Авто-управлението на силата по пулса е **изключено**, докато треньорът не го включи. Ключът е нов (`auto_reduce_v2`), затова старата стойност „включено“ не се пренася.
- Циферблатът не се отваря сам при ново стартиране на приложението.

## Навигация (v1.1.64)

Код: `widget/XemsNav.java`, кука: `scripts/apply-xems-nav.py`.

- **☰ горе вляво** отваря падащо меню с петте страници: Тренировка, Потребител, Настройки, Ръководство, План. Иконите и имената се взимат от старите табове. Текущата страница е маркирана в червено.
- **Долната лента** вече съдържа модулите, а не страниците: Таймер (кехлибарено), Музика (зелено), Пулс (червено), AI (оранжево). Всяка плочка показва живо състояние, обновено всяка секунда:

  | Модул | Състояния |
  |---|---|
  | Таймер | Отброява / Готов / Натисни за настройка |
  | Музика | Свири / Пауза / Спряна |
  | Пулс | ♥ 128 bpm / Свързване… / Гривната не слуша |
  | AI | ● kcal / Подготовка / Умна сесия |

  Активна плочка е с цветна рамка и плътен кръг, а пауза или подготовка е в кехлибарено.
- **Десният панел**: бутоните ♫ ⏱ ♥ AI и излишните разстояния около тях са скрити. Остават стоп, старт, +, главен, −.
- Плочката натиска скрития бутон, така че всеки модул запазва логиката си. Ако не сте на „Тренировка“, първо се превключва там.

Куки: `MainFragment.onCreateView` → `XemsNav.attach(root)`, `changePageFragment` → `XemsNav.onPage(id)`, `WearableSyncHelper.attachMasterPanel` → `XemsNav.onTrainingPanel(root)`. Всички са защитени с `XemsGuard`.

## Съкратено

| Махнато | Размер |
|---|---|
| `TimerPresetUiHelper.java` (падащо меню със записи) | 360 реда |
| XML на настройките на таймера, елемент за падащо меню, неизползван шаблон `AmountView` (`apply-interval-timer.py`) | ~80 реда XML |
| `scripts/apply-active-pause-segments.py` (не се извиква никъде) | 1531 реда |
| Стар редактор на блокове със системни `SeekBar` | заменен |

`IntervalTimerHelper` + `TimerPresetUiHelper` бяха 2414 реда. Сега са 1948, с повече функции.
