{
  "name": "xems-license-server",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test test/*.test.js",
    "db:migrate": "wrangler d1 migrations apply xems-license --remote",
    "db:migrate:local": "wrangler d1 migrations apply xems-license --local"
  },
  "devDependencies": {
    "wrangler": "^4.14.0"
  }
}
